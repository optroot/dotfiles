
# TODO check for clipboard compatibility
# TODO click should not reset scroll/copy mode

# Full reset
source-file "${HOME}/.reset.tmux"

# Helpful variables
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"

# Mode set
set-option -gw xterm-keys on

# Colors

set-option -g  default-terminal "xterm-256color-italic"
set-option -ag terminal-overrides ",xterm-256color-italic:Tc"

# Prefix
set-option -g prefix C-a

# Tmux options
set-option -g display-time 1000
set-option -g base-index 1
set-option -g renumber-windows

# Mouse Mode
# set-option -g mode-mouse on
# set-option -g mouse-resize-pane on
# set-option -g mouse-select-pane on
# set-option -g mouse-select-window on
set-option -g mouse on

# bind-key -T root PPage if-shell -F "#{alternate_on}" "send-keys PPage" "copy-mode -e; send-keys PPage"
# bind-key -t vi-copy PPage page-up
# bind-key -t vi-copy NPage page-down
#bind -n WheelUpPane copy-mode

# TODO bind ESC to copy-mode
# TODO fix Mouse only for tmux>v2.0
# arrow keys when not in vim?
# Get C-TAB and C-S-TAB working


#bind-key WheelUpPane if-shell "$is_vim" "send-keys -M" "copy-mode \; scroll-up"
#bind-key -n WheelUpPane copy-mode \; scroll-up \;
#bind-key WheelDown
# bind-key WheelUp
# bind-key WheelDown
#bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
#bind -n WheelDownPane select-pane -t= \; send-keys -M


#resize-pane -M

# bind-key WheelUp
# bind-key WheelDown
# bind-key MouseDown1
# bind-key MouseDown2
# bind-key MouseDown3
#
# bind-key MouseUp1
# bind-key MouseUp2
# bind-key MouseUp3
#
# bind-key DoubleClick1
# bind-key DoubleClick2
# bind-key DoubleClick3
#
# bind-key TripleClick1
# bind-key TripleClick2
# bind-key TripleClick3
#
# bind-key MouseDrag1
# bind-key MouseDragEnd1
# bind-key MouseDrag2
# bind-key MouseDragEnd2
# bind-key MouseDrag3
# bind-key MouseDragEnd3

# Title
set-option -g set-titles on
set-option -g set-titles-string '#T'

# Activity monitoring
set-window-option -g monitor-activity on
set-option -g visual-activity off

# Rename
bind-key A command-prompt -p "Rename:" "rename-window %%"
set-option -g allow-rename off
set-window-option -g allow-rename off
set-window-option -g automatic-rename off

# New Window
bind-key c new-window
bind-key C-c new-window
bind-key C command-prompt -p "Name of new window:" "new-window -n '%%'"

# Vi copypaste mode
set-window-option -g mode-keys vi

# Show the copy history
bind-key Space choose-buffer

# history
set-option -g history-limit 1000

# Copy-mode
bind-key Escape copy-mode
unbind-key p
bind-key p paste-buffer
bind-key -t vi-copy 'v' begin-selection
#bind-key -t vi-copy 'y' copy-selection

# Pane creation
# bind-key ARROWS TO RESIZE WINDOW
#bind-key | display-panes \; split-window -h
bind-key v display-panes \; split-window -h
#bind-key - display-panes \; split-window
bind-key s display-panes \; split-window

# Quick Resize
bind-key h resize-pane -L +10
bind-key j resize-pane -D +10
bind-key k resize-pane -U +10
bind-key l resize-pane -R +10

# Quick Reordering 
bind-key - swap-window -t -1
bind-key = swap-window -t +1
# not needed
bind-key 0 move-window -r

# Reload config
bind-key r source-file ~/.tmux.conf \; display-message "Config reloaded..."

# TODO bind C-TAB and C-S-TAB to tab switching
bind-key -n C-Tab next-window
bind-key -n C-S-Tab next-window

 
# THEME
set-option -g status-position top

# Status update interval
set-option -g status-interval 1

# Window separator
set-option -g window-status-separator ""

# Basic status bar colors
set-option -g status-fg colour240
set-option -g status-bg colour233

# Left side of status bar
set-option -g status-left-bg colour233
set-option -g status-left-fg colour243
set-option -g status-left-length 40
set-option -g status-left "#[fg=colour233,bg=colour39,bold] #S #[fg=colour39,bg=colour240,nobold]#[fg=colour233,bg=colour240] #(whoami) #[fg=colour240,bg=colour235]#[fg=colour240,bg=colour235] #I:#P #[fg=colour235,bg=colour233,nobold]"

# Right side of status bar
set-option -g status-right-bg colour233
set-option -g status-right-fg colour243
set-option -g status-right-length 150
set-option -g status-right "#[fg=colour235,bg=colour233]#[fg=colour240,bg=colour235] %H:%M:%S #[fg=colour240,bg=colour235]#[fg=colour233,bg=colour240] %Y-%m-%d #[fg=colour39,bg=colour240]#[fg=colour233,bg=colour39,bold] #H "

# Window status
set-option -g window-status-format "  #I:#W#F  "
set-option -g window-status-current-format "#[fg=colour233,bg=black]#[fg=colour81,nobold] #I:#W#F #[fg=colour233,bg=black,nobold]"

# Current window status
set-option -g window-status-current-bg colour39
set-option -g window-status-current-fg colour235

# Window with activity status
set-option -g window-status-activity-bg colour75 # fg and bg are flipped here due to
set-option -g window-status-activity-fg colour233 # a bug in tmux

# Window status alignment
set-option -g status-justify centre

# Pane border
set-option -g pane-border-bg default
set-option -g pane-border-fg colour238

# Active pane border
set-option -g pane-active-border-bg default
set-option -g pane-active-border-fg colour39

# Pane number indicator
set-option -g display-panes-colour colour233
set-option -g display-panes-active-colour colour245

# Clock mode
set-option -g clock-mode-colour colour39
set-option -g clock-mode-style 24

# Message
set-option -g message-bg colour39
set-option -g message-fg black

# Command message
set-option -g message-command-bg colour233
set-option -g message-command-fg black

# Mode
set-option -g mode-bg colour39
set-option -g mode-fg colour232

# set -g @plugin 'nhdaly/tmux-better-mouse-mode'

# List of plugins
set-option -g @plugin 'tmux-plugins/tpm'
set-option -g @plugin 'tmux-plugins/tmux-sensible'
# set-option -g @plugin 'tmux-plugins/tmux-resurrect'
# set-option -g @plugin 'tmux-plugins/tmux-continuum'
set-option -g @plugin 'tmux-plugins/tmux-yank'
set-option -g @plugin 'tmux-plugins/tmux-copycat'
set-option -g @plugin 'christoomey/vim-tmux-navigator'
set-option -g @plugin 'nhdaly/tmux-better-mouse-mode'

# Plugin Settings
# set-option -g @continuum-restore 'on'

# Plugin Overrides
bind-key C-a last-window
bind-key a send-prefix
bind-key C-l send-keys 'C-l'

# Paste from clipboard
# TODO make it faster
# TODO fix when clipboard broken?
# bind-key p                  run-shell "xsel --clipboard >~/.tmux-buffer-tmp" \; load-buffer -b tmp-copy-buffer ~/.tmux-buffer-tmp  \; if-shell "$is_vim" "send-keys yop" "paste-buffer -b tmp-copy-buffer -d" \; run-shell -b "rm ~/.tmux-buffer-tmp"
# bind-key C-v                run-shell "xsel --clipboard >~/.tmux-buffer-tmp" \; load-buffer -b tmp-copy-buffer ~/.tmux-buffer-tmp  \; if-shell "$is_vim" "send-keys yop" "paste-buffer -b tmp-copy-buffer -d" \; run-shell -b "rm ~/.tmux-buffer-tmp"
# bind-key C-p                run-shell "xsel --clipboard >~/.tmux-buffer-tmp" \; load-buffer -b tmp-copy-buffer ~/.tmux-buffer-tmp  \; if-shell "$is_vim" "send-keys yop" "paste-buffer -b tmp-copy-buffer -d" \; run-shell -b "rm ~/.tmux-buffer-tmp"


# set-option -g scroll-without-changing-pane on
# set-option -g scroll-in-moused-over-pane on

# TODO stay in copy mode
# bind-key -n MouseDown1Pane  
bind-key -n MouseDown1Pane select-pane -t =
bind-key -n MouseDrag1Border resize-pane -M


# TODO mouse_any_flag
bind-key -n MouseDown3Pane  run-shell "xsel --clipboard >~/.tmux-buffer-tmp" \; load-buffer -b tmp-copy-buffer ~/.tmux-buffer-tmp  \; if-shell "$is_vim" "send-keys yop" "paste-buffer -b tmp-copy-buffer -d" \; run-shell -b "rm ~/.tmux-buffer-tmp"
# bind-key -n MouseDown3Pane  run-shell "xsel --clipboard >~/.tmux-buffer-tmp" \; load-buffer -b tmp-copy-buffer ~/.tmux-buffer-tmp  \; if-shell "$is_vim" "send-keys yp" "paste-buffer -b tmp-copy-buffer -d" \; run-shell -b "rm ~/.tmux-buffer-tmp"

# TODO override copycat
#local file_search="$(get_tmux_option "$copycat_file_search_option" "$default_file_search_key")"
#tmux set-option -g C-f "(^|^\.|[[:space:]]|[[:space:]]\.|[[:space:]]\.\.|^\.\.)[[:alnum:]~_-]*/[][[:alnum:]_.#$%&+=/@-]*"


# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run-shell '~/.tmux/plugins/tpm/tpm'


